class CategoryModel {
    int id;
    String name;
    DateTime createdAt;
    DateTime updatedAt;

    CategoryModel({
        this.id,
        this.name,
        this.createdAt,
        this.updatedAt,
    });
}